#Dev environment

app:
  message: This is property file to the ${spring.application.nome} specific to Dev Environment

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
